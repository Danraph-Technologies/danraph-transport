// Sonner toast setup for React (for testing in signup form)
import { Toaster } from "sonner";

export default function SonnerToaster() {
  // You can customize position, duration, etc. here if needed
  return <Toaster richColors position="top-center" />;
}
